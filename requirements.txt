# Python deps for prep + training + ONNX export.
# Install:  pip install -r requirements.txt

# data prep & catalog
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # parquet reader for the HF dataset

# model & training
torch>=2.2
scikit-learn>=1.3      # AUC / logloss eval

# ONNX export + validation
onnx>=1.15
onnxscript>=0.1.0      # required by torch.onnx export on torch>=2.5
                       # (we also pass dynamo=False; see README export note)

# dataset download
huggingface_hub>=0.23